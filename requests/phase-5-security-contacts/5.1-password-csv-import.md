## Objective

Implement password manager CSV upload and parsing. Support 1Password, Bitwarden, and LastPass export formats. Parse into normalized records. Data held in React state only — NEVER persisted.

## Acceptance Criteria

- [ ] File upload UI for CSV
- [ ] Parse 1Password CSV format
- [ ] Parse Bitwarden CSV format
- [ ] Parse LastPass CSV format
- [ ] Auto-detect format from column headers
- [ ] Normalized record: account_name, username, password, url
- [ ] Data in React state only — verify no persistence
- [ ] Handle malformed CSV with error message

## Files to Touch

src/stations/security/SecurityStation.tsx

## Tests to Write

- Parse 1Password CSV format
- Parse Bitwarden CSV format
- Parse LastPass CSV format
- Auto-detect format correctly
- Malformed CSV shows error
- No Supabase calls in component (enforce no persistence)

## Dependencies

1.2, 1.4 (CRT design, Shell)
