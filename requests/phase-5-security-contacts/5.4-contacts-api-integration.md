## Objective

Implement Google Contacts API integration (or CSV import as fallback). Fetch user's contacts with name, email, phone. Store in Supabase contact_records table.

## Acceptance Criteria

- [ ] Fetch contacts from Google Contacts API (People API)
- [ ] Fallback: CSV import for contacts
- [ ] Extract: name, email, phone, source
- [ ] Store in Supabase contact_records table
- [ ] Handle large contact lists (pagination)
- [ ] Client-side contacts.ts service

## Files to Touch

src/services/contacts.ts, src/stations/contacts/ContactsStation.tsx

## Tests to Write

- Contacts fetched from API
- CSV import parses correctly
- Records stored in Supabase
- Pagination handled

## Dependencies

1.6, 1.7 (auth, API structure)
