## Objective

Implement lazy loading for stations: each station loaded only when navigated to. Code splitting via React.lazy + Suspense. Optimize bundle size.

## Acceptance Criteria

- [ ] Each station is a lazy-loaded chunk
- [ ] React.lazy + Suspense with CRT-styled fallback
- [ ] Initial bundle contains only homepage + shell + boot
- [ ] Bundle analysis shows separate chunks per station
- [ ] No perceptible delay on station navigation

## Files to Touch

src/App.tsx, vite.config.ts (chunk splitting)

## Tests to Write

- Lazy components load on navigation
- Suspense fallback renders
- Bundle has separate chunks (build test)

## Dependencies

All stations built
