## Objective

Initialize Vite + React + TypeScript project. Install core dependencies: react-router-dom, @supabase/supabase-js, googleapis, @anthropic-ai/sdk. Configure tsconfig (strict mode), Vitest, React Testing Library, Prettier, ESLint. Create directory structure per spec. Set up .env.example with all required vars. Create vercel.json with SPA rewrite rule. Create initial Supabase table migration SQL files.

## Acceptance Criteria

- [ ] `npm run dev` starts Vite dev server
- [ ] `npm run build` produces production build
- [ ] `npm run test` runs Vitest
- [ ] `npm run lint` runs ESLint
- [ ] `npm run format` runs Prettier
- [ ] TypeScript strict mode enabled
- [ ] .env.example contains all required var names
- [ ] vercel.json has SPA rewrite and serverless config
- [ ] Directory structure matches spec

## Files to Touch

package.json, tsconfig.json, vite.config.ts, vercel.json, .env.example, .prettierrc, .eslintrc.cjs, src/main.tsx, src/App.tsx, src/utils/types.ts

## Tests to Write

- Smoke test: App renders without crashing
- Config test: TypeScript strict mode is enabled

## Dependencies

None — this is the first request.
