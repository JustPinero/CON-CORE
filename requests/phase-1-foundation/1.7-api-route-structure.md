## Objective

Create the API route skeleton: all route files with method validation, Zod request schemas, consistent {data, error, meta} response shape, CORS headers, error handling pattern. Create /api/health endpoint. Create Supabase client utility for serverless functions. Create env var validation with Zod (validate at function cold start).

## Acceptance Criteria

- [ ] /api/health returns { data: { status: "ok" }, error: null, meta: { timestamp } }
- [ ] All route files have method validation boilerplate
- [ ] Zod schemas defined for request validation
- [ ] Consistent response shape across all routes
- [ ] CORS headers set on all routes
- [ ] Supabase server client utility created
- [ ] Env var validation runs at cold start, exits on failure
- [ ] scripts/validate-env.sh validates locally

## Files to Touch

api/health.ts, api/gmail/*.ts (skeleton), api/calendar/*.ts (skeleton), api/claude/*.ts (skeleton), src/utils/api-response.ts, src/utils/supabase-server.ts, src/utils/env-validation.ts

## Tests to Write

- /api/health returns 200 with expected shape
- Response utility produces correct shape for success
- Response utility produces correct shape for error
- Env validation fails on missing required vars

## Dependencies

1.1, 1.6 (scaffolding, auth)
